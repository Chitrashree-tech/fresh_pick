name: fruit_admin_dashboard
description: A Flutter admin dashboard to manage fruits, vegetables, flowers, and orders.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # Supabase for backend connection
  supabase_flutter: ^1.10.17

  # Cupertino icons for iOS-style design
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
